import type { RequestGameState } from "../types/game";

export function sceneFragmentPrompt(
  gameState: RequestGameState,
  sceneId: string,
  mode: "dialogue" | "fragment",
  storyContext: string,
  scenePrompt: string,
  requiredLines: string[]
) {
  const exampleScript = [
    "[旁白]示例旁白内容。",
    "",
    "[NPC:刘宇]示例NPC台词。",
    "",
    "[主角说]示例主角台词。",
    "",
    "[旁白]示例收束动作。",
  ].join("\\n");

  const requiredLinesBlock = requiredLines.length > 0
    ? `10. 以下台词必须逐字出现在script中，不得改动任何标点或文字：\n${requiredLines.join("\n")}`
    : "10. 没有指定必说台词，不要重复同一句话。";

  return [
    // ── 身份与任务 ──────────────────────────────────────────
    "你是视觉小说《快乐小孩》的动态剧情生成模块，负责生成一小段剧本编码文本。",
    `当前场景ID：${sceneId}`,
    `生成模式：${mode === "fragment" ? "AI片段（多角色动态小场景）" : "AI对话（NPC主导）"}`,
    "",
    // ── 输出格式（最高优先级）──────────────────────────────
    "═══════════════════════════════════",
    "【输出格式 — 最高优先级，不可违反】",
    "═══════════════════════════════════",
    "你的完整回复必须是且只能是下面这种合法JSON对象，不得在JSON前后附加任何文字、标点或换行：",
    "",
    `{"script":"${exampleScript}"}`,
    "",
    "script字段规则：",
    "  · 只能使用以下角色码开头的段落：[旁白]  [主角]  [主角说]  [NPC:角色名]",
    "  · 每个段落之间用字面量 \\n\\n 分隔（即JSON字符串内两个反斜杠n）",
    "  · 禁止在JSON之外输出任何内容，包括markdown代码块、说明文字、道歉语句",
    "  · 禁止直接扮演角色说话——你输出的是JSON，不是对话",
    "  · 如果不确定格式，请参考上方示例结构",
    "",
    // ── 剧情规则 ────────────────────────────────────────────
    "剧情规则（在满足输出格式前提下遵守）：",
    "1. 必须逐字保留场景要求中的必说台词，并遵守固定事实、禁止事项和行数范围。",
    "2. 不得创造新的关键世界观事实，不得改变死亡、许可、线索、主线结果或下一场景。",
    "3. 根据choiceHistory最后一次相关选择、七维人格、NPC信任和flags调整语气与反应。",
    "4. 片段必须有完整收束：NPC对主角的安慰/帮助/质问/交易必须至少回应一句，随后用[旁白]交代动作或情绪变化。",
    "5. 若行数范围与完整收束冲突，优先保证剧情完整，尽量控制在范围上限附近。",
    requiredLinesBlock,
    "",
    // ── 游戏状态 ────────────────────────────────────────────
    "玩家当前状态：",
    JSON.stringify(gameState, null, 2),
    "",
    // ── 世界观 ──────────────────────────────────────────────
    "全局世界观底座：",
    "《快乐小孩》是规则副本视觉小说。主角叶平生被困在学校/家庭等区域规则中，表层规则背后指向「好孩子」、服从、成绩、家庭期待和精神匮乏等主题。AI只能补写当前小场景的对话、旁白和简单动作，不能提前揭秘根本规则，不能改变主线结果，不能把NPC写成普通现实世界人物而忘记副本规则压力。",
    "",
    // ── 剧情上下文 ──────────────────────────────────────────
    "主线剧情上下文：",
    storyContext.trim() || "无额外上下文。仍需严格依据本场景剧本要求，不要脱离《快乐小孩》的学校/家庭规则副本语境。",
    "",
    // ── 本场景要求 ──────────────────────────────────────────
    "本场景剧本要求：",
    scenePrompt,
    "",
    // ── 输出格式再次强调（末尾锚定）────────────────────────
    "▶ 再次确认：现在请直接输出JSON对象，第一个字符必须是 { ，最后一个字符必须是 } ，中间只有script一个键。",
  ].join("\n");
}
